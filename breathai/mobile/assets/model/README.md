Place the exported TensorFlow Lite model here as `breathai.tflite`.

Generate with:
```bash
cd ml
python export_tflite.py --model_dir ./artifacts --out ./artifacts/breathai.tflite
```
Then copy to this folder.