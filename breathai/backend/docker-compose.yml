version: "3.8"
services:
	mongo:
		image: mongo:7
		restart: unless-stopped
		ports:
			- "27017:27017"
		volumes:
			- mongo_data:/data/db
	backend:
		build: .
		env_file:
			- .env
		ports:
			- "8080:8080"
		depends_on:
			- mongo
		restart: unless-stopped
volumes:
	mongo_data: